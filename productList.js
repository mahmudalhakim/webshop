$(document).ready(function () {
    productList.init();
})

let productList = (function () {
    let init = function () {
        renderProducts();
    }

    function renderProducts () {
        
    }

    return {
        init: init
    }
})();

